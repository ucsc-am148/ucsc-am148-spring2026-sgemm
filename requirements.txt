# Python deps for the SGEMM assignment. Install with:
#     pip install -r requirements.txt
# Requires Python 3.12 and a system-installed CUDA 12.x toolkit.

numpy==2.4.4
numba==0.65.0
numba-cuda==0.30.0

# Optional: only needed if you tackle the K6 stretch goal in
# kernel6_stretch.cu (loaded via cupy.RawKernel).
cupy-cuda12x==14.0.1
